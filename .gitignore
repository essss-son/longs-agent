# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Runtime data: sessions / skills / traces / tool-output (per-user, never tracked)
.agent/sessions/
.agent/skills/
tool-output/

# Local config: keep example tracked, ignore real (may hold base_url)
.agent/config.toml

# IDE / OS
.vscode/
.idea/
.DS_Store

# Internal planning doc (not for remote)
longs-agent项目说明.md

# Claude Code local config
.claude/

# All markdown docs (internal, not for remote)
*.md

# SWE-bench evaluation (code, logs, results) — not for remote
.agent/swebench*
.agent/run_all.log
agent_quiz/
eval_report.json
logs/
new_test.py
